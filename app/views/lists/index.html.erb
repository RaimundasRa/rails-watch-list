<br>
<div class="container">
<h1>All lists</h1>
<% @lists.each do |list| %>
<%= link_to list.name, list_path(list) %>
<% end %>
<br>
<%= link_to 'Create a new list', new_list_path(@list) %>
<br>
</div>
